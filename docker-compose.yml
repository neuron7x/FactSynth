services:
  api:
    build: .
    image: ghcr.io/${COMPOSE_PROJECT_NAME:-factsynth}/factsynth:dev
    ports: ["8000:8000"]
    environment:
      - ENV=dev
    command: uvicorn factsynth_ultimate.app:app --host 0.0.0.0 --port 8000
