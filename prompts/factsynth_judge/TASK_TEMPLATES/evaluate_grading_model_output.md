Given: (A) gold VERDICT JSON, (B) model VERDICT JSON.
Compute: exact-match on `VERDICT`, macro-F1 over `support` labels, and calibration check (penalize `certain` on wrong items). Return a score card.
