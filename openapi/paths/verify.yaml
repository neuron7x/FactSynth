post:
  summary: Verify claim and lock
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/VerifyRequest.yaml
  responses:
    '200':
      description: Verification result
      content:
        application/json:
          schema:
            $ref: ../components/schemas/FactSynthLock_v1_1.yaml
