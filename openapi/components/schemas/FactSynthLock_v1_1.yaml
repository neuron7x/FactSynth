type: object
required:
  - version
  - nonce
  - api_guard
properties:
  version:
    type: string
    enum: ["1.1"]
  nonce:
    type: string
  api_guard:
    type: string
    enum: ["factsynth-lock/v1.1"]
