[pytest]
addopts = -q -ra
testpaths = tests
pythonpath = src
filterwarnings = ignore::DeprecationWarning
markers =
    anyio: async tests
    smoke: stream smoke tests
    slow: lengthy streams/benchmarks
