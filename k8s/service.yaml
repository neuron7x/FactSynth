apiVersion: v1
kind: Service
metadata: { name: fsu-api, namespace: factsynth, labels: { app: fsu-api } }
spec:
  selector: { app: fsu-api }
  ports: [{ port: 80, targetPort: http, protocol: TCP, name: http }]
  type: ClusterIP
