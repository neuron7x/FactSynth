apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: fsu-allow-ingress, namespace: factsynth }
spec:
  podSelector: { matchLabels: { app: fsu-api } }
  policyTypes: [Ingress]
  ingress:
    - from: [ { namespaceSelector: {} } ]
      ports: [ { protocol: TCP, port: 80 } ]
